services:
  db:
    image: postgres:15
    container_name: sweetroom-db
    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    ports:
      - "5432:5432"
    volumes:
      - sweetroom-postgres-data:/var/lib/postgresql/data
    restart: unless-stopped

volumes:
  sweetroom-postgres-data:
